# npm publishing configuration
registry=https://registry.npmjs.org/
access=public
engine-strict=true

# Prevent accidental publishing to the wrong registry
# For scoped packages: @doc2html-arch/doc2html-arch
# For public packages: leave access=public above